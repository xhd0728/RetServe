# Embedding models
infinity-emb>=0.9.0

# Vector indexing
faiss-gpu>=1.7.4; sys_platform != "darwin" or platform_machine != "arm64"
faiss-cpu>=1.7.4; sys_platform == "darwin" and platform_machine == "arm64"

# Data processing
numpy>=1.21.0
jsonlines>=3.1.0
tqdm>=4.65.0
orjson>=3.9.0

# Web services
fastapi>=0.100.0
uvicorn>=0.23.0
pydantic>=2.0.0
pydantic-settings>=2.0.0
python-dotenv>=1.0.0

# Configuration
pyyaml>=6.0

# OpenAI client
openai>=1.0.0

# Type hints
typing-extensions>=4.6.0
